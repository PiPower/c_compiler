// Type your code here, or load an example.
float x, y;
double x1, y1;
int main(int num) {

    int p;
    p = x == y;
    p = x <= y;
    p = x < y;
    p = x >= y;
    p = x > y;
    p = x != y;

    p = x1 == y1;
    p = x1 <= y1;
    p = x1 < y1;
    p = x1 >= y1;
    p = x1 > y1;
    p = x1 != y1;
}