unsigned int x, b, c, g;

int main()
{
    char x2 = x;
    long int y = -230000000000000000, z = 21;
    unsigned ppr = -2;
    unsigned long pdpr = 68 * 3  * (293 + 32 - 4) / (long)x + y;
    pdpr =  (unsigned long) -(int)pdpr;
    unsigned int d = x + (unsigned int)y;

    x = b = (long)c *2 + 23 * (long)(g = 2);
    int ds = 231;
    ds = x;
    return 2;
}